# publish

1. Call `review_context`.
2. Call `review_publish`; pass `local: true` to publish a tuicr session first, and pass the requested event when supplied.
3. Report submission and draft-ready state.
