# merge

1. Local mode is not accepted; only remote reviews can merge.
2. Call `review_context` and confirm the PR is open, non-draft, mergeable, and green.
3. Ask for a conventional squash subject only when it cannot be derived.
4. Call `review_merge` and report the resulting merge.
