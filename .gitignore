# Operating System Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Editor and IDE
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace
.vs/

# Build outputs and dependencies
dist/
build/
# ...but keep mise task files that live under a build/ namespace dir
!**/mise-tasks/build/
!**/mise-tasks/build/**
out/
target/
bin/
obj/
node_modules/
bower_components/
vendor/

# Zig build artifacts
zig-out/
.zig-cache/

# Package manager files
*.lock
# Rust: keep the committed lock for reproducible git-tag/`cargo install --git`
# consumers (rust template ships Cargo.lock; the broad *.lock above would hide it).
!Cargo.lock
# Bun: keep the committed lock for reproducible CI installs (bun.lock is text).
!bun.lock
package-lock.json
yarn.lock
pnpm-lock.yaml
Gemfile.lock
Pipfile.lock
poetry.lock
composer.lock

# Packed npm tarballs (npm pack / bun pm pack output)
*.tgz

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment and secrets
.env
.env.local
.env.*.local
.env.registry
*.key
*.pem
*.p12
*.pfx
*.keystore
*.jks
secrets.yml
credentials.yml
secrets.json
credentials.json
service-account*.json
*.crt
*.cer
id_rsa*

# Cache and temporary files
*.cache
.cache/
tmp/
temp/
*.tmp
*.temp
.sass-cache/
.npm/
.yarn/
.tmp/

# Coverage and test reports
coverage/
*.cover
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.nyc_output/
test-results/

# Backup files
*.bak
*.backup
*.old

# mise local configuration (user-specific overrides)
.mise.local.toml

# Instance-specific development files
.spec
.claude/*
!.claude/CLAUDE.md
!.claude/commands/
!.claude/commands/**

# Style guide learning data (personal, not shared)
.style/patterns.json
.style/history.jsonl
# .style/config.json is committed (team settings)

# Claudevoyant style guide learning data (personal observations)
.claudevoyant/style/patterns.json
.claudevoyant/style/history.jsonl
# Keep config (team settings)
!.claudevoyant/style/config.json

# Pi agent session data (local runtime, not shared)
.pi/
